# Modele wygenerowane (zestawy B, C, D)

Tu trafią pliki `.scad` i profile JSON dla nowych zestawów krzywek B, C, D,
generowane na bazie wymiarów zestawu A i realnych wzorów ściegów.

Puste do czasu uzupełnienia danych źródłowych — patrz `../../docs/NEEDED_INPUTS.md`.
